#include <cstdlib>
#include <time.h>
#include <iostream>
#include <cmath>

int main() {
    srand(time(NULL));

    // for (int i=0; i < 100; i++) {
    //     std::cout << rand()%30 << "  ";
    // }
    if (sqrt(3000000) == exp(3000000)) {
        std::cout << exp(3000000) << "  ";

    }

    std::cout << std::endl;
    return 0;
}